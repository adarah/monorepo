name: Verify

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          echo "$GOOGLE_SERVICE_ACCOUNT" > /tmp/credentials.json
          bazel build --remote_cache=https://storage.googleapis.com/bazel_cache_bucket_adarah --google_credentials=/tmp/credentials.json //...
        env:
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}

      # This is a hack to work around the upload-artifact action not working with symlinks
      # See: https://github.com/actions/upload-artifact/issues/92
      - name: Dereference bazel-bin symlink
        run: echo "UPLOAD_PATH=$(readlink -f bazel-bin)" >> $GITHUB_ENV
      
      - name: Save bazel-bin artifact
        uses: actions/upload-artifact@v3
        with:
          name: bazel-bin
          path: ${{ env.UPLOAD_PATH }}

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test
        run: |
          bazel test //...

  plan:
    needs: build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infrastructure

    if: github.event_name == 'pull_request'
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      HEROKU_EMAIL: ${{ secrets.HEROKU_EMAIL }}
      TF_VAR_discord_token: ${{ secrets.DISCORD_TOKEN }}
    steps:
      - uses: actions/checkout@v3

      - name: Download bazel-bin artifact
        uses: actions/download-artifact@v3
        with:
          name: bazel-bin
          path: bazel-bin/

      - uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: ~1.1

      - name: Terraform format
        id: fmt
        run: terraform fmt -check -recursive
        continue-on-error: true
        working-directory: ..

      - name: Terraform init
        id: init
        run: terraform init

      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color
        continue-on-error: true

      - name: Terraform plan
        id: plan
        run: terraform plan -no-color
        continue-on-error: true

      - uses: actions/github-script@v6
        env:
          PLAN: "stdout:\n${{ steps.plan.outputs.stdout }}\nstderr:\n${{ steps.plan.outputs.stderr }}"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
            #### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
            #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
            <details><summary>Validation Output</summary>

            \`\`\`\n
            ${{ steps.validate.outputs.stdout }}
            \`\`\`

            </details>

            #### Terraform Plan 📖\`${{ steps.plan.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`\n
            ${process.env.PLAN}
            \`\`\`

            </details>`;
              
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1